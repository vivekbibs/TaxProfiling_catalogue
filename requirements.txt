name: taxprofiling-catalogue

channels:
  - conda-forge
  - bioconda

dependencies:
  # ── Python ────────────────────────────────────────
  - python=3.12

  # ── Streamlit ─────────────────
  - streamlit>=1.35
  - streamlit-pdf         

  # ── Dev tools ─────────────────────────────────────
  - black
  - isort
  - mypy
  - pylance          # via pip 
  - pre-commit

  # ── Data / utils ────────────────────────────
  - requests
  - pandas           
  - pydantic        

  # ── Tests ─────────────────────────────────────────
  - pytest
  - pytest-cov

  # ── pip  ───────────────────────
  - pip
  - pip:
    - pylance
    - streamlit-extras     